apiVersion: v1
kind: Service
metadata:
  name: {{ include "marsad.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "marsad.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "marsad.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
